version: '3.8'

services:
  annotation-validator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: annotation-validator
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
